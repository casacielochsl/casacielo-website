.vercel
node_modules
.env
.env.local
.env*.local

# Original high-res source images (2MB+ each, CMYK) — optimized web/email
# versions actually used by the site live in assets/ instead.
/Images

# Resident CSV with real names/contact info — already imported into MongoDB,
# should not sit in git history.
/memberlist
